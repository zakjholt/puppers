{
  "name": "puppers",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "dev": "parcel src/index.html",
    "predeploy": "rm -rf dist && parcel build src/index.html --public-url /puppers",
    "deploy": "gh-pages -d dist"
  },
  "author": "zak holt",
  "license": "ISC",
  "browserslist": [
    "last 1 Chrome version"
  ],
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "parcel-bundler": "^1.12.4"
  },
  "dependencies": {
    "normalize.css": "^8.0.1"
  }
}
